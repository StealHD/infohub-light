<!-- init-pro:control schema=3 profile=backend project=inteliscope-infohub-light file=docs/decisions/records/D151-D175.md -->
# D151–D175 决策记录

<a id="d151"></a>
### D151 手动信息流刷新按实时角色限域，并采用协作式安全停止

- 决策日期：2026-08-13
- 当前状态：本任务分支实现与验证中；不新增 API 路径或数据库迁移。
- 决策内容：手动 `user_feed_refresh` 的来源范围由服务端在创建和 Worker 执行时分别按实时角色决定，客户端 payload 不能扩大范围。Member 只处理自己拥有且已启用的 private 来源；Owner/Admin 处理自己的 private 及自己订阅的 public/workspace 来源；Viewer 无权创建。无有效范围订阅时，在创建 Job 和记录配额前以 `409 no_enabled_subscriptions` 回滚。相同 Feed 控件在 queued/running 时转为安全停止：queued 立即取消；running refresh 只持久化 `cancelled_at` 并保留 claim，Worker 在安全边界丢弃结果，避免 Feed、Source Health、计划或通知副作用。发布事务已开始时保持原子完成。
- 原因：浏览器提交的范围提示不能作为授权来源，旧“所有启用订阅”的模糊语义会让 Member 触及公共来源；同时强杀 Worker 会破坏正在进行的网络调用、claim 和原子发布。双重角色收口与持久化停止标记把权限、队列幂等、可观测状态和副作用边界收进同一条 Job 生命周期。
- 兼容/边界：保留既有 Job API、队列 schema 与 retry 入口；summary 仅增加可选 `cancelled_at`。客户端只据此显示等待状态，不能自行判定权限或停止 Worker。收藏缓存、阅读布局和静态 bootstrap 不影响服务端授权语义。

<a id="d152"></a>
### D152 ActorOps 固定三槽采用服务端槽位操作与证据保留

- 决策日期：2026-08-13
- 当前状态：任务分支实现与验证中；本地迁移和真实付费验证仍须按独立运行/确认边界执行。
- 决策内容：ActorOps 继续固定为 `primary/backup_1/backup_2` 三槽，不引入动态池。服务端为每槽投影新增、替换、移出以及稳定阻断原因；新增只能填第一个空槽，替换只能处理已占用槽。新增和替换沿用人工候选、一次付费验证和一次无费用原子生效，Stage/Canary 额外冻结操作槽、基础池、Route generation、候选精确 Revision/Build/Manifest、来源指纹及费用上限。验证期间 active pool 保持不变，替换成功后旧 Revision 进入 superseded 而非删除。
- 移出边界：移出使用精确确认词和 CAS，只移除 active-slot 引用并将余下槽向前压紧；Revision、Canary、费用和审计历史全部保留。unknown-start、活动 attempt/freshness/Stage、来源或 generation 冲突、以及压紧后低于 Route 实际 Actor/发布者门槛一律零写入拒绝。移出不得调用 Actor；只有仍匹配的 source Canary 证据可把 binding 降为相应 `ready_1of1|ready_2of2`，不伪造新验证。
- 迁移与界面：`apify_actor_pool_management_v22` 使用 global schema 24，离线迁移需要停 API/Worker、heartbeat 安全窗、无活跃 ActorOps Job、`0600` backup、marker/shape/integrity/foreign-key 检查及失败恢复。ActorOps 局部采用无阴影、无 sticky blur 的平面槽位与页签；pending 时锁定路由、页签和弹窗退出，390 px 自然单列。
- 原因：原有三槽池只能在抽象的升级路径中变更，管理员无法有界地移出、补回或替换单个已验证 Actor；把槽名或可否操作交给浏览器又会绕过当前任务、来源、证据和 CAS 边界。将意图限定为服务端的固定槽操作，能够复用已验证的 Stage/Canary 安全模型，同时以不硬删历史保证可审计与可恢复。

<a id="d153"></a>
### D153 槽位重规划必须保持服务端目标可见，超额费用立即终态拒绝

- 决策日期：2026-08-14
- 当前状态：任务分支实现与验证中；不新增 schema 或放宽 Actor、来源、身份和费用门槛。
- 决策内容：持久化 `add_slot|replace_slot` Stage 的工作流投影使用其真实 goal 和 `operation_slot`，页面用该槽读取候选；已关闭的本地弹窗和 legacy 升级状态均不得改写这项服务端意图。上游终态费用若超过批准上限，validation、attempt 与 batch 以同一个安全原因终结并保留实际费用，不得卡为 running、激活 Revision 或自动重试。
- 原因：把单槽重规划误显示成旧池升级会让管理员看到与候选接口不同的操作，无法验证或纠正真实状态；将实际超额费用留在“运行中”则同时破坏账本、操作锁和费用保护。明确投影与单一终态把 UI、CAS、审计和下一次人工确认重新对齐。

<a id="d154"></a>
### D154 ActorOps 候选先经免费可执行性证明，精确零费用中止可恢复未知启动

- 决策日期：2026-08-14
- 当前状态：任务分支实现与验证中；不增加数据库 schema，也不放宽费用、身份、内容或 unknown-start 的安全门槛。
- 决策内容：兼容 Candidate 在显示为可选择之前，必须免费证明公开可运行、非 deprecated、价格受限、精确成功 Build、可验证 input/output Schema、受控 target/max-items 输入、官方 input validation 与 Route 内容语义。X Profile 额外拒绝 reply/search/contact/其他平台等非 profile 内容语义；历史 Revision 缺少新证明时只显示稳定原因，不能进入付费计划。原始 POST 已回传 Run ID 而本地登记失败时，只有中止该精确 Run 后 GET 确认 `ABORTED` 与 `$0`，才把原 reservation 结算为终态审计行并解除锁；不得用账户列表猜测、不得把未知或非零费用当零、不得自动重发。
- 原因：把只在付费 Canary 才会失败的 Actor 放进“可选”列表会让管理员反复支付无效验证，也掩盖了输出合同或输入映射的确定性缺陷。另一方面，已知的零费用中止 Run 不是不明远端副作用；保留该精确证据并结算原 reservation，既避免永久锁死，又不牺牲重复扣费防护。

<a id="d155"></a>
### D155 ActorOps 以认证槽位手动主用和公开 Store 质量排序收口管理员选择

- 决策日期：2026-08-14
- 当前状态：任务分支实现与验证中；不新增数据库 schema，不自动启动或重试付费 Actor。
- 决策内容：三条固定 Route 都允许管理员将已占用的备用槽设为主用。服务端以 Route generation、活动 Stage/attempt/freshness 与 unknown-start 栅栏保护该操作；确认后只原子交换现有 slot/candidate 顺序，保留 Revision、来源验证、费用与审计，不创建 Actor Run。Route 的未来单 Run 上限可在同一 CAS 边界内调整至最高 `$0.10`，不改动当前池也不主动消费额度。候选先保持所有公开性、Build、Schema、输入、内容合同、价格与身份底线；仅在通过后，才按 Apify Store 的公开评分、评分人数和使用人数稳定排序并投影给管理员，任何缺失指标均不得伪造。
- 原因：管理员需要能直接切换已验证备份来确认真实运行链路，而不应为顺序调整重新触发验证或付费。Store 的质量指标能帮助从同样安全的候选中做出更可解释的选择，但若参与安全准入或替代免费预检，会再次把热门但不可执行的 Actor 误导为可配置项。
- 兼容/边界：初配、新增、替换、移出和来源启用仍使用既有两阶段确认与证据门槛；YouTube 仍 Atom-first，Actor 只在允许的 fallback 失败路径执行。全局 `$1` 测试额度不成为产品自动消费授权，任何付费 POST 仍需此流程内的人工确认。

<a id="d156"></a>
### D156 YouTube 以来源认证 Actor 为主、Atom 仅作失败降级

- 决策日期：2026-08-17
- 当前状态：本任务分支实现与验证中；不新增数据库 schema 或自动付费。
- 决策内容：保留 YouTube 的 RSS 来源身份、稳定内容 ID 与公开网络约束，但只有已完成来源级 Canary 的绑定可抓取。每次抓取先冻结并执行该来源的认证 Actor，按发布时间倒序保留来源设置的条数；Actor 本次失败才尝试公开 Atom/RSS，认证 Actor 的空结果不回落到 Feed。Actor 结果重新投影为 RSS 条目时必须保留 Route generation、来源、候选和水位证明，缓存与发布水位据此识别为 Actor 获取。新增或改目标的 YouTube 来源会绑定 Route；验证前允许订阅但以明确错误阻止获取。
- 原因：Atom/RSS 的网络超时、解析及上游限制曾被写成抓取成功且没有新内容，而已认证 Actor 又只在其后才有机会执行，无法提供稳定的最新订阅。把认证证据作为主链准入并将 Atom 限为同次失败降级，可让“成功”对应真实内容合同与可追溯 Actor，同时保留无费用的临时可用路径。
- 兼容/边界：已有来源不会被迁移或删除；其绑定一旦完成现有来源级 Canary 即自动使用 Actor 优先路径。每次运行仍遵守 Route 单次费用上限、冻结 Build/Manifest、身份/内容/发布时间合同和已有切备规则；本变更不创建 Canary 或周期新鲜度任务，付费验证仍需现有明确确认。

<a id="d157"></a>
### D157 ActorOps 只向浏览器暴露已完成实测与对账的目录

- 决策日期：2026-08-18
- 当前状态：本任务分支实现与验证中；不新增数据库 schema，不自动扩大费用额度。
- 决策内容：免费 Discovery 及受控 Canary 的原始库存仅保留在服务端，用于继续筛选、失败记忆和受控实测。`pool-candidates` 的浏览器投影只包含已通过精确 Build Route Canary、所有当前启用来源的匹配指纹 Canary、且全部费用完成对账的 Actor。浏览器选择 opaque Candidate 后只执行无费用、CAS 保护的原子启用或替换；证据或 generation 变化则零写入拒绝，绝不重复调用 Actor。
- 原因：此前把“可进行下一次试跑”的内部库存当成了“可供管理员选择”的目录，导致用户在选择后才看到元数据、身份或输入合同失败，也可能误以为仍会再次付费。将实测与选择分离后，用户可见候选的语义与“现在可直接运行”一致，同时保留后台对不同平台各自执行器、输入映射、输出验证和费用规则的受控处理。
- 兼容/边界：X、Instagram、YouTube 只共用 Stage、证据、CAS 与目录编排；平台组合的执行器、输入、输出与费用策略必须预先登记，否则 fail closed。旧失败/费用/Discovery 记录保留审计，不回写成候选；选择前后的任何证据变化均不产生新费用。

<a id="d158"></a>
### D158 ActorOps 能力矩阵统一标准主备，并免费恢复中断的既有 Canary

- 决策日期：2026-08-18
- 当前状态：本任务分支实现与验证中；不新增数据库 schema，不创建新的自动付费授权。
- 决策内容：X、Instagram、YouTube 的注册能力矩阵都显式绑定平台执行器、输入映射、输出验证、费用策略与标准门槛 `2 Actor / 2 发布者`；第三槽仍按需补充。未登记 tuple 在任何写入或调用前拒绝。Worker 在无活动 Stage 时免费收口旧 Route/binding 到该策略；活动 Stage 保留冻结的 Build、目标槽和来源证据，待其安全结算。Worker 重启后，已知远端 Run 的 validation、batch item、batch、stage 与 Job 必须一起投影为 `blocked_unknown_start`，随后只能 GET 核对原 Run/Dataset 和原费用；核对成功时续跑原审批的冻结 batch。若原 Run 已终态失败，则仅继续同一审批内尚未运行的条目，不再次 POST 已失败 Actor；仅当 Route/Profile/Stage 共同证明版本号只被该未知启动保护步骤自动加一时，才恢复冻结审批版本。若旧 Worker 已把同批零费用项误标为 `approval_stale`，只在同一证据链完整时将其重新排队；任何真实配置变更仍拒绝。重建前必须以真实运行库的残留状态完成回归演练。
- 原因：之前 YouTube 的单路 fallback 例外和重启时只更新 validation 的行为，使容量、选择目录、Stage 与 Job 互相矛盾，出现“一路显示可用、一路仍在运行”以及无法补齐备份。统一最小池与原 Run 的免费恢复把可用性、费用和 UI 真相收回到同一证据链。
- 兼容/边界：D155 的 Atom-first 描述及 D156 的 Atom 降级被本决策取代；YouTube 保留 RSS/频道 ID 仅作为来源身份，运行时只走认证 Actor。浏览器目录仍仅显示成功、对账且完成全部已启用来源验证的 Revision；用户选择不触发 Canary 或收费。网络不可读、超时或未结算继续阻断，绝不猜测成功或重新启动。

<a id="d159"></a>
### D159 ActorOps 自动替换退役并回归免费发现与双确认

- 决策日期：2026-08-20
- 当前状态：本任务实现与验证中；不新增运行时 schema，不删除任何既有 Actor、费用或实验表数据。
- 决策内容：单槽新增/替换只使用现有 `pool-candidates/refresh → Canary plan/batch → staged activate` 链路。一次管理员动作至多创建一个免费 Discovery Job，内部最多三轮 Store 查询；付费必须先展示冻结 Actor、Route generation、逐项和批次上限并完成确认 1/2，费用终结且 Stage `apply_ready` 后仍需确认 2/2 才能改变活动池。未发布的 auto-pool HTTP、Worker continuation、循环补位、服务端代填 approval/apply 与自动生效全部退役。
- 费用与迁移边界：所有 Route 单 Run 写入与执行硬上限统一为 `$0.10`。现役 ActorOps migration 链止于 global 24；实验性 global 25 若已存在只作惰性历史证据，不参与 runtime、readiness、maintenance 或 fresh bootstrap，普通代码不得读写、迁移或删除。只有显式离线退役工具可在 API/Worker 停止、heartbeat 过窗、无 unknown-start 且费用全部终结后安全终态化遗留 auto-owned 工作；global 25 永久保留，未来 migration 从 26 继续。
- 原因：自动流程曾绕过现有双确认、缺少幂等与唯一 active 约束，并在 `$0` shortfall 下无限创建 Discovery；其费用账本同时漏记成功批次且可重复累计。复用已经具备 plan hash、approval ID、CAS、费用终态和恢复语义的现役 Stage 链，能消除第二套不完整状态机，同时保留管理员的一键免费搜索体验。
- 兼容/边界：已存在的 global 25 marker、表和历史行不删除、不回写为新候选，也不成为未来迁移前置。旧客户端访问 auto-pool 路径得到统一 404。YouTube、Instagram 与 X 的标准门槛继续为两个 Actor、两个发布者；任何真实 Actor/AI/通知调用仍只允许在既有明确授权边界内发生。

<a id="d160"></a>
### D160 ActorOps v2 采用稳定获取优先与按订阅能力注册的适配器架构

- 决策日期：2026-08-20
- 当前状态：Phase 3 的默认关闭数据面、只读 Reconciler 与 Worker claim 隔离已实现；Discovery、站立授权和平台切流尚未实现或启用，现役平台继续遵守 v1/D159。
- 决策内容：ActorOps v2 将来源获取数据面与 Actor 发现/验证/维护控制面分离。一个 ready Actor 即允许获取并标记 degraded，两个以上才为 healthy 补池目标；单 Attempt 的 unknown start 只冻结本记录和费用预留，不能阻断其他 Route、免费来源或整个 Workspace Key Pool。数据面按 Active、Standby、Last Known Good、Adapter 明确支持的免费原生降级执行，Publication fence 只保护本来源目标、binding 和实际 Candidate。Discovery 分阶段持久化且确定性优先，AI 失败不丢弃已安全候选；统一 Reconciler 只读取和结算既有 Run，不创建新 Run、不回退 generation、不重开终态。
- 架构边界：通用行为进入 `src/services/actorops/` 的 domain、ports、registry、repository、policy、runtime、discovery、reconciliation、maintenance 与薄 service。每个 `platform + target_type + capability` 注册独立 `ActorRouteAdapter`；YouTube Channel Items、X Profile Items、Instagram Profile Items 分开实现，未来同平台的新订阅类型继续独立注册并只共享平台 helper。Adapter 只负责目标、发现能力、Actor 输入、输出身份/内容映射和可选原生降级；SQL、Candidate 选择、Key/预算、状态、Job、恢复与 Feed 发布留在通用层。通用模块禁止平台条件分支、Mixin 和 `globals().update()`；冻结巨型文件只允许缩小。
- 自动维护：v2 站立授权默认关闭，管理员一次启用后默认每 Probe `$0.05`、每 Route 每 UTC 日 5 次、Workspace 每 UTC 月 `$3.00`。额度内可自动验证、加入 Standby 并替换非最后一个不健康 Actor；永不自动移除最后一个可运行 Actor，超预算、身份不确定或 Publisher 风险升级仍需人工确认。维护预算与正常来源抓取费用分账。
- 迁移与兼容：global 26 以显式离线 migration 建立 v2 事实表并从 global 17–24 只读 backfill；global 25 永久惰性且不读写。现有 `/api/admin/apify-*` 在逐平台切流期间作兼容 facade。现役代码和测试已经在来源认证 Actor 失败后执行受限 Atom/RSS 降级，本决策立即把该事实恢复为 v1 合同且不改变运行行为；v2 继续沿用，X/Instagram 无可信免费路径时明确 unsupported。YouTube、Instagram、X 按顺序独立 shadow、观察和切流，旧表在平台验收前保留只读回退快照。
- 取代关系：本决策关于 YouTube 当前降级事实的说明立即取代 D158 的“只走认证 Actor”文字；在某个平台启用 v2 前，D159 的免费发现与双确认仍是现役付费/生效合同。启用 v2 后，本决策再针对该平台取代 D158 的 2/2 运行硬门槛和全局恢复耦合，也取代 D159 对站立授权内自动补位/替换的长期禁止。D154 的免费预检与精确远端证据、D157 的安全公共投影、global 25 惰性边界及所有 SecretStore/费用审计要求继续有效。
- 原因：现役实现将平台映射、状态机、费用、恢复和 Worker 前置维护集中到多个巨型模块和约三十张相关表，造成控制面不确定直接阻断订阅数据面；按平台注册 Adapter 仍把平台分支散入通用 Runtime/Discovery，新增订阅类型会继续扩大单体。单一通用流程、严格端口和局部故障域能让新增平台只增加适配代码，同时把产品最高不变量从“任何不确定都全局阻断”改为“单点不确定不得停止已有稳定获取”。

<a id="d161"></a>
### D161 global 26 只迁移已收敛摘要，不接管 v1 inflight

- 决策日期：2026-08-20
- 当前状态：Phase 1 已实现并通过定向验证；global 26 是默认停用的 Domain/storage 基础，不接入 API、Worker 或来源运行时。
- 决策内容：fresh DB 在现有 v1 seed 完成后自动安装 global 26 七表、单调 trigger、三条 Route 摘要和 disabled maintenance policy；existing global 24 只能运行显式离线 `scripts/migrate_actorops_v2.py`。迁移在备份前拒绝活动 Worker、所有非终态 ActorOps Job/Stage/Batch/Validation/Attempt/Run、unknown start 和远端费用未结；因此只 backfill 当前 Route、exact Candidate、Binding/LKG 与最近成功/失败摘要，v2 Attempt、Discovery 和费用账本从空表开始。完整 v1 审计历史保留只读，不建立长期双写。
- 原因：把 v1 inflight 同时复制到尚未接管 Worker 的 v2 会产生两个恢复所有者，无法证明哪套状态机可以终态化原 Run、费用和 approval。先让 v1 收敛再复制稳定事实，能把 global 26 保持为可重复、无网络、无费用的数据库事务，并把真正的重启接管留给后续单一 Reconciler。
- 迁移安全：version 26 精确依赖有效 global 24，不要求也不查询 global 25；partial v2 表、占用 version、错误 checksum/shape 均 fail closed。apply 创建 UTC `0600` backup，在一个事务中建表、backfill 和写 marker，随后验证 v1 内容摘要、schema shape、integrity 与 foreign keys，失败恢复原库。blocked/already-migrated 不创建 backup。
- 兼容/取代：本决策取代 Phase 0 inventory 中“backfill 非终态 Attempt/Run 供 v2 Reconciler 收敛”的计划，不改变 D160 的最终局部 unknown-start 与单一 Reconciler 目标。existing v24 缺少 global 26 且 flag 关闭时 v1 API/Worker 仍 ready；Phase 2 已增加条件 gate，真实流量接管仍留到逐平台切流。

<a id="d162"></a>
### D162 ActorOps v2 数据面使用双门切换和无状态 Adapter 合同

- 决策日期：2026-08-20
- 当前状态：Phase 3 已实现并默认关闭；所有 Route 保持 disabled，未执行真实 Actor 或平台切流。
- 决策内容：`ACTOROPS_V2_ENABLED=false` 时 runtime/readiness 不查询 global 26；开启后 API/Worker 才要求完整 marker/shape。Route `disabled` 继续 v1，`shadow` 只读冻结 v2 选择后继续 v1且不写 Attempt、不新增 Actor POST，`active` 才进入 v2。通用 Runtime 固定执行 Active、Standby priority、去重 LKG 和 Adapter 明确支持的免费降级；可信空结果终止链路，Candidate 失败才切备，credential/remote unknown 不惩罚 Candidate且 remote unknown 不再启动第二个付费 Candidate。
- 端口修正：Candidate 的输出合同依赖其 immutable Manifest 与 FetchWindow，因此无状态 `ActorRouteAdapter.validate_output` 明确接收这两个参数；Adapter 不通过实例可变状态传递一次调用上下文。X Profile Items、Instagram Profile Items、YouTube Channel Items 分开注册且不继承通用工作流，只有 YouTube Adapter 注入受公共网络约束的 Atom 降级。Repository 保留一个公开类，并以 focused `repository_*` 模块分担 SQL，避免重新形成单体。
- 发布与恢复边界：Attempt 在任何付费 POST 前持久化；Apify coordinator wrapper 截获 registration、known-run reconcile 与 unknown-start，但不调用 v1 workspace 级 barrier。成功远端事实先终结 Attempt，来源 target fingerprint、binding version 和实际 runnable Candidate 在 Feed 事务内再次验证后才推进 LKG/水位；assignment、Route/Key Pool generation 和其他 Route 变化不使内容失效。同一 logical attempt 的未终态或未结算重放返回 recovery-required，已结算重放返回 stable already-settled，均绝不重复 POST；未知/已知 Run 的读取与结算由 D163 的单一 Reconciler 负责。
- 原因：仅有 schema 和 Adapter 名称不能保证数据面在并发切备、缓存复用与远端未知启动时保持局部故障域。双门让代码先部署而不切流，无状态 Adapter 参数保证 exact Manifest 验证可审计，局部 publication proof 与 coordinator wrapper 则防止 v1 的全局 generation/barrier 再次把一个来源故障扩大到整个 Workspace。

<a id="d163"></a>
### D163 ActorOps v2 对账只结算既有事实，并与 Worker claim 隔离

- 决策日期：2026-08-20
- 当前状态：Phase 3 已实现并默认关闭；全部 Route 仍为 disabled，未运行真实 Actor、来源或平台切流。
- 决策内容：Reconciler 只枚举 global 26 中已存在的 `start_unknown|registered|running` Attempt，以及费用未终结的已知终态 Run。通用层只能使用 RemoteRunLedger Port；Apify Adapter 以 `logical_run_id=attempt_id` 精确关联 durable reservation，并且只允许 GET、状态/费用结算和空窗口证明。它不得 POST、retry、abort、按列表猜测或接管 Dataset/Feed。已知远端成功若缺 publication proof，以 `actorops_reconciled_unpublished_success` 终结 Attempt；仅精确账户空窗口证明可将 unknown start 以 `$0` 终结；歧义、读取失败或费用未终结保持待对账并以安全错误更新排序。
- Worker 边界：迁移 gate、失效 lease 恢复和常规 schedule 入队仍在 claim 前；Provider/v1/v2 reconcile 迁至 Job 已终态后的 housekeeping，或 idle 时的有界 maintenance 后。每个 workspace、组件和 Attempt 独立处理异常，控制面错误不得阻断已 claim 的普通来源 Job，也不新增 Job 类型、队列表或 schema。
- 原因：远端不确定不能通过再次启动付费 Actor 自愈，也不能把未发布 Dataset 伪装为已有 Feed；而把旧 Provider 对账放在普通 Job claim 前会让局部密钥或 Actor 故障扩大成全局获取延迟。将远端事实结算与 publication 分开，并在 claim 后隔离维护，保留审计和费用正确性，同时维持稳定免费/普通获取。
- 兼容/边界：flag=false 不读取 global 26/global 25，也不运行 v2 Reconciler；flag=true 仍要求完整 global 26 shape。D162 的双门、Runtime 重放边界和局部 publication fence 继续有效；Discovery checkpoint、维护授权、API/UI 投影和任何 active/shadow 观察留给后续 Phase。

<a id="d164"></a>
### D164 ActorOps v2 Discovery 以安全 checkpoint 和确定性优先恢复

- 决策日期：2026-08-20
- 当前状态：Phase 4 已实现并默认关闭；全部 Route 仍为 disabled，未运行真实 Store、AI、Actor 或平台切流。
- 决策内容：Discovery 复用 global 26 的 Job 与 Candidate 关联表，不增加 migration。Store Search、Metadata、Build/Pricing/Schema、Mapping、Ranking、Persist 每个边界都以 status/stage/generation CAS 写入有界安全 cursor 与 canonical hash；cursor 仅含 Actor/Build/Schema hash、Candidate ID、rank 和安全 rejection code，禁止真实 target、Prompt、模型原文、Secret 和 Provider 原始 metadata。中断后从当前阶段读取 exact Build 再核对 hash，发现 drift 只安全拒绝该 revision，绝不回退 stage。确定性映射先创建 inactive `static_valid`，不确定映射保留 inactive `mapping_pending`，确定性失败记录 inactive `rejected`；任何 Build/Manifest 变化新建 Candidate，不 UPDATE immutable revision。
- AI 与 Worker 边界：Adapter 对每个订阅能力独立声明 Store query 和 Schema-proven Manifest 映射。AI 只接收无值的公开 Build/Schema 摘要来补 unresolved mapping，结果必须再次以 exact Schema 验证；AI 不可用、超时或幻觉字段只留下 `mapping_pending`，不能阻断已确定的 Candidate 或让 Job 永久失败。v2 使用独立 `actorops_v2_discovery` Job，idle housekeeping 只入队已存在的 queued/retry/running 事实；flag=false 不读 global 26/global 25，v1 Discovery Job 和其表不变。Discovery 不启动 Actor、Probe、Dataset、费用 reservation 或 Route assignment。
- 原因：只保存一个 hash 无法在 Worker 重启后证明下一次 metadata/AI 输入仍对应同一 Build，而保存原始上游 metadata、目标或 Prompt 又会扩大敏感数据和 schema。安全引用加 exact re-read 让恢复可验证，Candidate immutable 事实防止重放改写历史；将 AI 变为非关键增强和 Worker Job 独立化，使目录控制面不会恢复 v1 的全局阻断或隐式收费。

<a id="d165"></a>
### D165 ActorOps v2 站立维护采用双授权、单 Probe 与最后一路保护

- 决策日期：2026-08-20
- 当前状态：Phase 5 已实现并默认关闭；没有 HTTP API/UI，全部 Route 仍为 disabled，未运行真实 Actor、来源、迁移、AI 或平台切流。
- 决策内容：站立维护只有 workspace 与 Route policy 同时由 enabled Owner/Admin 以 generation CAS 授权后才可运行。每个低优先级 Job 先免费精确核对公开 Actor、publisher、Build、Schema hash 和价格；随后在一个事务内重验 policy、Route、Candidate、Binding/target fingerprint，按每 Route 每 UTC 日 5 次、单次 `$0.05`、Workspace 每 UTC 月 `$3.00` 的已结算实际费用加未结预留原子准入，并确保同 Route 同时至多一个未结 Probe。Probe 限制为一个 Candidate、一个输出项目和一次远端启动，禁止 native fallback、Feed/Dataset 恢复及 LKG/水位更新；未知启动仍交由 D163 的只读 Reconciler。
- Candidate 与 assignment：可信非空 Probe 才写入成功证据，并由 `static_valid → probationary → certified` 的既有单调状态推进；可信空结果为 no-evidence，预检/身份/预算失败不花费，Candidate 合同失败才写入 Candidate 级失败。成功的 runnable Candidate 优先补齐 Standby；已有不健康 assignment 时只能在另一 runnable 路径仍存在时原子隔离并重排 Active/Standby。最后一个 runnable Candidate 永不自动移除，记录 `actorops_maintenance_last_candidate_protected` 且保持 assignment。
- Worker 与兼容边界：flag=false 不读取 global 26/global 25，也不入队维护；flag=true 时仅在 Job 终态后或 idle housekeeping 有界入队，不得阻塞普通 claim。此阶段不改 global schema、现役 v1 API/Worker/readiness、Route runtime mode 或平台流量；maintenance policy 的 API/UI、人工操作投影与 Platform shadow/cutover 继续留给后续阶段。
- 原因：自动付费补位若只依赖单个 Route 开关或异步读预算，容易被过期授权、并发 reservation 或异常 Candidate 放大为费用和可用性事故。将免费 exact proof、双授权、单 Probe ledger 与 assignment CAS 收在同一局部边界，能在不接管 v1 流量时验证维护机制，同时把“绝不失去最后一路”作为写入级不变量。

<a id="d166"></a>
### D166 ActorOps v2 以离线 Route CAS 和逐平台费用授权切流

- 决策日期：2026-08-20
- 当前状态：Phase 6 的代码级护栏已实现；2026-08-21 已在本地完成 global 26 migration，尚未运行 shadow、active、真实来源、付费 Actor 或 Worker 重启验收，全部 Route 仍为 disabled。
- 决策内容：Route mode 只能以 generation CAS 相邻变更 `disabled → shadow → active` 或 `active → shadow → disabled`。前进前必须停止 API/Worker 并跨 heartbeat 窗口、确认本 Route 无 in-flight/start-unknown/未结费用，且 marker/shape、v1/v2 Route/slot/Binding 摘要、runnable Candidate 与 Binding readiness 全部有效；`active → shadow` 是正常紧急回退，立即阻止新 v2 获取但不撤销既有远端事实，D163 Reconciler 仍只读结算且绝不恢复发布。
- 操作与费用边界：`scripts/actorops_v2_cutover.py` 的 status/snapshot/transition/verify 命令是唯一离线组合入口，默认 transition 不写入；snapshot 仅创建 `0600` SQLite backup 与不含 target、Secret、Manifest 原文的安全摘要。每个平台按 YouTube、Instagram、X 顺序独立计算并等待确认 `20 × 冻结 runnable Candidate 数 × Route per_run_cap_usd`，不得共享余额。任何候选顺序、cap 或 binding 变化均使观察窗口作废并要求重新 snapshot/确认；常规回退只改 mode，数据库 backup 只用于损坏或 migration 失败。
- 兼容与身份：shadow 继续 v1 且只记录无值选择事件，不创建 v2 Attempt 或额外 POST。active 的 X/Instagram 绑定来源只经其 v2 Adapter；YouTube RSS/Atom 路径识别 v2 handle，保留原 RSS feed/video stable ID。X/Instagram 共用 `platform + source_key + native_id` 身份，因此 v1/v2 切换不得制造重复 Feed 内容。flag、Compose 和示例配置仍默认 false；不新增 HTTP API、React UI、global schema、平台环境开关或 maintenance 授权。
- 原因：数据库层直接跨级切换或用一次全局额度覆盖三类平台，会让一个未知费用、错误 binding 或重复 POST 扩大为不可回滚的全局事故。将 mode 变更限定为离线 CAS、把证据与费用冻结到单平台观察窗，并让回退只停止新流量，可保留已有 Feed/Attempt 审计且隔离平台风险。

<a id="d167"></a>
### D167 历史 Actor 费用以保留最坏暴露的离线证据隔离

- 决策日期：2026-08-21
- 当前状态：Phase 6R 已在本地运行数据库完成经确认的历史费用 quarantine 与 global 26 migration；未切换任何 Route。
- 决策内容：历史 terminal Run 只能通过精确 authenticated GET 的 `usageTotalUsd` 写入实际费用并标记 final。远端 404 是“历史记录已不可读取”的证据，不是零费用证明：保留原 reservation、`cost_final=false`，仅写 `apify_historical_cost_quarantined` 并纳入 `historical_unknown_upper_bound_usd`。401/403/429/5xx/timeout、unknown-start、多个/模糊 reservation 或任何非终态事实一律继续阻断。无 durable Run 的 terminal Attempt 以全额预留和独立 code 隔离；遗留 running Batch 只有无 active Job、无非终态 remote Run 后才可同事务标记 partial/unrecoverable，绝不 requeue、retry 或启动 Actor。
- 操作边界：`scripts/audit_actorops_v2_legacy_costs.py scan --evidence` 的 reader 只允许对至多 20 个已知 Run 发 GET，不读取 Dataset、不 POST、不 abort；`--resume` 在同一 `0600` 加盐 session 中只选未覆盖事实，既有阻断观察必须显式 `--retry-blocked` 才能重读并替换。`snapshot` 不联网、不生成 salt：它只在 API/Worker 停止、heartbeat 过窗后验证所有远端/本地事实仍受 evidence 覆盖，再创建 `0600` SQLite backup 和单独 receipt。apply 同时需要 receipt、evidence hash 与精确最坏费用确认，并在一个状态/更新时间 CAS 事务中拒绝 drift。脚本、证据和 stdout 不包含 target、Secret、Manifest 或 remote Run ID。迁移仅在安全 code、terminal state 和必要关联都同时匹配时排除 blocker，不能把 quarantine 当作结算或跳过其他 v1 收敛门禁；global 26 的七表和 global 25 惰性边界保持不变。
- 原因：Apify 可能按保留期删除历史 Run；把 404 或账户聚合账单伪装成单 Run `$0` 会低估真实暴露并破坏迁移审计。保留最坏 reservation、让操作者针对可证实的批次确认，并把不可恢复 Batch 显式终态化，才能既解除永久无解的旧账本阻塞，又不把不确定性转化为虚假的费用事实。

<a id="d168"></a>
### D168 ActorOps v2 管理面采用 additive facade 与默认关闭策略 CAS

- 决策日期：2026-08-21
- 当前状态：Phase 7 的 API/UI 收敛已实现为默认关闭的代码路径；未更改 Route mode、未创建 Probe、未启动 Actor 或执行真实来源获取。
- 决策内容：既有 `/api/admin/apify-routes*` URL 保持 v1 payload 和权限语义，只有 `ACTOROPS_V2_ENABLED=true`、global 26 marker 和完整 shape 同时成立时才 additive 投影 v2 health、runtime mode、Active/Standby/LKG、最近成功、降级原因和脱敏维护策略。`GET|PATCH` workspace/Route maintenance policy 使用 Owner/Admin 和 generation CAS；enable 只记录授权，不排队、不预留且绝不启动 Probe。v2 Settings 视图只读取这些通用字段，刻意不渲染旧 Discovery、Stage、Batch 或多重 generation。flag=false 不访问 global 26，且新 policy URL 返回稳定 unavailable 错误而非隐式建表或兼容写入。
- 原因：在 Route 仍可回退 v1 的切流期间替换 URL 或让 UI 继续耦合旧 Batch/Stage，会把展示变化扩大为执行变化。将 v2 读模型置于 additive、受 readiness 约束的 facade 下，并把策略 mutation 收窄为零费用 CAS，既保持旧客户端可用，也允许管理台在真实切流前独立验收新的健康模型。

<a id="d169"></a>
### D169 global 26 只接受单一已结算远端 Run 作为旧 Attempt 费用证明

- 决策日期：2026-08-21
- 决策内容：旧的 Worker 重启可能在远端 Run 已以准确实际费用结算后，遗漏关联 terminal Attempt 的 `cost_final` 派生位。global 26 的离线 blocker 只在该 Attempt 恰好关联一个含 remote ID 的 terminal Run，且该 Run 的 `charge_final=1`、`charge_actual_usd` 非空时接受其为精确证明；迁移不回写旧 Attempt，也不复制任何 v1 Attempt/Run/费用到 v2。多个 remote Run、没有最终实际费用、非终态或缺 remote ID 均继续 fail closed。
- 原因：采用单个已结算 Run 的持久化事实可以修复纯派生位丢失，同时精确计数禁止把双启动、歧义 reservation 或未知费用解释为已结算，保持 D167 的最坏暴露和只读迁移边界。

<a id="d170"></a>
### D170 目录社交订阅缺失旧 binding 时以离线 pending bridge 修复

- 决策日期：2026-08-21
- 决策内容：global 26 的原始 backfill 只能复制 `apify_source_route_bindings`，因而会遗漏已存在、但尚未进入旧 ActorOps Route 的 `apify_social` 目录订阅。backfill 与 `scripts/repair_actorops_v2_catalog_bindings.py` 均只选择没有 v1 或 v2 binding 的来源，使用其 `platform/kind` 得到已登记 RouteKey，再由对应 Adapter 重新校验 target；通过后创建确定性 ID、`status=pending`、`binding_version=1`、`source_v1_generation=1` 的 v2 binding。它不写 source catalog、v1 binding、Candidate assignment、LKG、水位、Attempt、费用或 Route mode，也不把来源内容/目标输出到 stdout。已安装 global 26 的 repair 必须停 API/Worker、跨 heartbeat 窗口，apply 创建 `0600` backup，在一个 SQLite 事务中只插入仍缺失行，检查 integrity/foreign keys，失败恢复 backup。切流摘要只认可能从当前目录配置重新推导的 bridge；它仍不满足 ready，不能绕过 shadow/active 的证据和费用门槛；global 25 保持完全惰性。
- 原因：目录订阅存在与旧 ActorOps binding 存在不是同一事实。用 pending bridge 保留用户的订阅与规范 target 身份，可以修正迁移遗漏，又不会把历史的普通获取成功伪造成某个 v2 Candidate 的验证、LKG 或实际切流授权。

<a id="d171"></a>
### D171 切流只比较当前可执行 revision，并以 settled 来源证明解锁既有 binding

- 决策日期：2026-08-21
- 决策内容：v1 active slot 会保留终态、quarantined 或字段不完整的历史项，故切流摘要与 binding readiness 只能比较 v1 runtime 当前可调用的 exact revision：Candidate state 为 `closed|half_open|probationary`、revision lifecycle 为 `probationary|certified` 且 Build/Manifest 完整，顺序固定为 primary、backup_1、backup_2。`legacy_readiness.py` 要求该顺序逐项等于 v2 Active/Standby assignment，并要求每条 revision 均有当前 source/target 的 settled source-canary 成功证明；已有 v1 binding 才能由离线 CLI 以 status/version/fingerprint CAS 从 pending 提升为 ready。该操作不回写 v1 pool、不会重开终态，也不提升没有 v1 binding/proof 的 catalog bridge。首次本地 YouTube shadow 的 v2 选择事件成功且零 v2 Attempt/POST，但随后的 v1 Job 因仅一条 runnable revision、低于旧路由两个 Candidate 阈值而在网络前失败；按 D166 这不是 shadow 通过，active 被阻止，Route 必须恢复 disabled。
- 原因：过滤历史不可执行 slot 防止产生虚假的 v1/v2 drift；同时把 v1 的实际 Job 结果保留为 active 门槛，防止 v2 只读选择成功掩盖旧运行面已经失效。恢复 YouTube 观察窗口必须采用独立受测的 v1 pool 恢复操作，或先显式修订此决策，不能静默降低阈值或切到 active。

<a id="d172"></a>
### D172 实际 Canary 的 AI 字段映射必须逐精确 Revision 证明

- 决策日期：2026-08-21
- 决策内容：当一个已免费筛选、已冻结 Build 的 Canary 因输出字段映射而出现 `contract_mismatch` 或 `metadata_only` 时，Worker 可对该次进程内 Dataset 的无值 RFC 6901 路径与 JSON 类型摘要请求一次全局 AI 的 output-pointer proposal。proposal 只能选择摘要中已有路径，并必须具备该平台固定的内容 ID、内容 URL、发布时间、文本/标题及来源身份字段；代码以同一行的目标身份、host、内容、时间和窗口再次映射验证。通过后只将该 Actor + Build + 新 Manifest 的不可变 Revision 保存为成功合同；任何 Actor/Build/平台之间不得复用 pointer，AI 无效/不可用保持原确定性失败。Dataset 值、target、Prompt、模型原文和远端原始 metadata 均不落库、不进日志，也不发送给模型。
- 原因：Actor 的字段名和嵌套结构确实各不相同，但让模型凭“看起来像”决定来源身份会把推荐、跨账号或元数据污染进 Feed。把 AI 限为每个 exact Revision 的无值字段选择器，再由确定性输入验证和真实返回复验，既能适配异构 Actor，又保留可审计、不可跨合同误用的安全边界。

<a id="d173"></a>
### D173 可配置 Canary 上限与真实费用证明一致

- 决策日期：2026-08-21
- 决策内容：Route future-run 与 Candidate validation 的硬上限由 `$0.10` 提高至 `$0.20`，默认 Candidate profile 仍为 `$0.02`、默认批次预算不变。每次请求仍以 Route cap、批准的 profile cap、精确 Build/Manifest、目标身份、一次 POST 与实际 final charge 为独立栅栏；超过批准 cap 的 Run 继续终态失败，绝不因上限提高自动重试或激活。
- 原因：本地真实 Canary 已证明内容合同可通过，但同一次远端 Run 的最终实际费用为 `$0.11897`，旧硬上限会将已验证的 Actor 永久挡在晋级之外。将上限仅扩大到经授权的 `$0.20` 能容纳这一已知成本，同时保留一次性、CAS、费用结算和人工确认边界。
- 迁移边界：global 27 `apify_actor_validation_cap_v27` 只离线重建 v1 Canary settings、batch 与 item ledger 的费用 CHECK 为单 Candidate `$0.20`、批次 `$0.60`。它要求停 API/Worker、heartbeat 安全窗、无活跃 Actor Job、`0600` backup 与 postcheck；不读 global 25，不改变 global 26、Route mode、内容、来源、Run 或历史费用。缺 marker 时既有数据库保留 `$0.10`，更高 cap 在网络前失败。
- 精确 Revision：一次 final `contract_mismatch` 后，同一 Actor、Build 与 Manifest 不得被新的 Discovery 重开、重关联或再次批准；只有 Build/Manifest 变化才是新的不可变验证合同。这样候选列表、计划和实际执行共享同一终态事实，不会把已知失败伪装成可验证新 Actor。
