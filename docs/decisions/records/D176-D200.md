<!-- init-pro:control schema=3 profile=backend project=inteliscope-infohub-light file=docs/decisions/records/D176-D200.md -->
# Decision Records D176–D200

<a id="d176"></a>
### D176 ActorOps 以 global 30 完成 v2 单轨运行面

- 决策日期：2026-08-23
- 当前状态：本地实现与定向验证完成；未部署、未执行真实 Actor/AI/付费来源、未创建 Tag。
- 决策内容：global 30 是唯一的 ActorOps 在线 schema 门。API、来源 CRUD/调度/抓取、Worker、UI 和管理兼容 alias 只组合 v2 Route、Binding、Candidate、Attempt、Discovery、Maintenance、Replacement、共享 Run/Key Pool/alerts 与脱敏 Operation Log；Route 状态只允许 `active|disabled`。缺少 global 30 时仅 ActorOps API/Job 返回 migration-required，普通 RSS/GitHub/source fetch/user feed refresh 继续工作。`ACTOROPS_V2_ENABLED`、shadow、disabled→v1、v1 Canary proof、v1 Factory/Worker handler/前端控制面均删除。新数据库直接由 Adapter Registry 创建 disabled v2 Route、Binding schema、默认 maintenance policy 与共享 alerts，不构造 v1 Service 或历史表。
- 历史边界：v1 表、global 13–28 migration 形状、费用审计与退役工具只允许离线 migration/audit/retirement 读取；不 DROP 表、不重写历史费用。SQLite authorizer 的在线 deny 集覆盖全部历史 v1 表，明确保留共享 Run、Key Pool 与 alerts。未开始且零费用的四类 v1 Job 可标记 `actorops_v1_retired`；已领取、可能启动、unknown-start 或未结费用的事实保持隔离，必须经离线工具和精确操作员确认处理。
- 费用与恢复：Attempt 的持久 logical identity、唯一键、固定窗口和一次 POST 上限继续生效。已观察 Dataset 只 GET/replay；unknown-start 只交 Reconciler GET/结算，不启动 Standby 或 native fallback；已知费用和 final 标志单调补充，不能被失败路径降级。disabled X/Instagram 不创建 Attempt 或远端 POST；YouTube 只可走 Adapter 明确声明的免费、公共网络受控 RSS fallback。
- 取代关系：本决策取代 D162、D166、D168、D171、D174 中所有 feature flag、shadow、additive facade、v1 proof、v1 fallback、旧 Pool/Canary UI/Worker 及逐平台双轨切流语义；这些记录保留为历史理由。D160 的 Adapter/局部故障域、D163 的只读对账、D167 的历史费用隔离、D169 的精确费用证明、D175 的显式 v2 Replacement 费用授权以及所有 SecretStore/费用审计约束继续有效。
- 原因：双轨运行会让同一来源的 Binding、候选、费用和远端 POST 有两个潜在所有者，导致重复抓取、旧数据回退和不透明的 v1 兼容分支。收敛为一个 schema marker、一个 v2 binding 生命周期和一次远端启动边界，能在保留历史审计的同时让普通免费来源与 ActorOps 局部故障隔离。

<a id="d177"></a>
### D177 OpenClaw 专属架构先行，通用 Agent Core 延后

- 决策日期：2026-08-22
- 当前状态：已实施；只重构既有 OpenClaw、Remote MCP 与本地安装入口，不改变用户可见行为或运行合同。
- 决策内容：先以显式 `OpenClawChatController`、状态/Event/Port 合同、单一 Gateway Event Router、纯 Domain Projection、根 Reducer、Workbench Adapter、Remote MCP 三类 registrar 和 focused read/diagnostic/setup 模块稳定现有 OpenClaw 边界。历史 import 路径保留为显式兼容 façade，17 个 Remote MCP 工具、OpenClaw Protocol、scope、权限、数据库与部署保持不变。通用 Agent Core、capability registry、新 scope、新工具、用户配置体检、采集缺口检测、补采计划与核验、其他 Agent Adapter 延后到本重构合并并稳定后的独立分支和计划。
- 原因：当前只有一个已投入使用的 Browser Agent 适配器；在尚未稳定事件、状态、存储、Workbench 和 MCP 接缝前抽取通用 Core，会把 OpenClaw 特有协议误固化为全局抽象，并把纯结构重构扩大成 capability、授权和数据面的同时变更。先建立可执行的单向依赖与兼容 façade，后续才能用第二个真实 Adapter 需求验证通用接口。
- 兼容/边界：本决策不授权新增 Agent capability、工具、scope、数据库、迁移、ActorOps、Worker、Job、采集、AI、通知或发布操作。任何后续通用化都必须重新评估公共合同、权限、威胁边界和迁移策略，不得在 OpenClaw façade 内暗中扩展。

<a id="d178"></a>
### D178 产品文档按用户影响维护，不以机械 diff 代替审查

- 决策日期：2026-08-23
- 决策内容：删除要求任意产品代码合并同时修改 Manual 和 Changelog 的 Test Gate。产品变更仍须人工判断用户影响：可见行为或 API 兼容性变化更新 Changelog，操作或配置流程变化更新 Manual；内部重构、测试和 CI 不产生虚假文档修改。
- 取代关系与原因：本决策取代 D057 的双源强制 diff 门禁，不改变两份文档作为用户说明来源的职责。文件修改不能证明内容已被有效审查，反而会诱发无意义的 Reviewed 标记；将判断收回到变更评审，保留有实际用户价值的文档更新。

<a id="d179"></a>
### D179 OpenClaw 订阅覆盖全部用户来源且付费平台先建立停用绑定

- 决策日期：2026-08-23
- 当前状态：本地实现与定向验证完成；未部署、未执行真实 Actor/AI/抓取、未创建 Tag。
- 决策内容：“所有源”分为两层。任何当前用户可见且启用的目录来源都可通过既有 `mode=existing` 订阅，不按存储类型设白名单；新建并订阅覆盖 RSS/Website、Bilibili、YouTube channel、GitHub release/user、Reddit subreddit/user、Telegram channel、Hacker News、X profile 与 Instagram profile。Agent 公开类型保留兼容别名，规范新类型为 `github_user`、`reddit_user`、`instagram`、`hackernews`；通用 `apify` 仍只允许订阅既有可见来源。OpenBB 与 OSS Insight 尚未产品化为用户目录来源，不在本阶段伪装成可创建来源。
- 事务与费用边界：X/Instagram 的 apply 在同一 `BEGIN IMMEDIATE` 中创建 private source、用户订阅和 ActorOps v2 Binding。Binding 固定从 `pending` 开始，source 与 schedule 保持 disabled，subscription 关系可以 enabled；该事务不创建 Attempt/Job，不执行抓取、Probe、Actor POST、AI 或通知。管理员必须在 Web ActorOps 中完成既有核验与启用流程，OpenClaw 不获得 activation、费用或额外 scope。Binding 创建失败时业务行全部回滚，proposal 保持 pending 并返回稳定的暂不可用错误。
- 兼容与输入：Remote MCP 仍精确为 17 个工具，认证、scope、proposal snapshot v2、十分钟过期、逐来源 prepare/精确确认/apply 和数据库 schema 均不变。X/Instagram、GitHub user 与 Reddit user 只接受确定性的公开账号名、`@handle` 或官方主页；裸 `@handle` 未说明 X/Instagram 时由 Skill 询问一次，不猜测、不开放网页发现。YouTube 和 Bilibili 继续使用各自既有的受控解析流程。
- 架构边界：Agent 扩展类型与规范化归 `agent_source_extensions.py`，核心 `source_type_registry.py` 保留兼容 façade；Web 与 MCP 共用 `services/actorops/source_lifecycle.py`。本决策是 D177 明确要求的后续独立 capability 切片，不改变 Browser OpenClaw event/state 架构，也不引入通用 Agent Core。

<a id="d180"></a>
### D180 X 账号订阅默认排除断上下文回复

- 决策日期：2026-08-24
- 当前状态：本地实现与定向验证完成；未执行真实 Actor、付费抓取、历史修复或部署。
- 决策内容：`x/profile/items` 的用户内容语义收口为非回复帖子流。X Adapter 先以完整 Dataset 执行既有 immutable Manifest、目标身份和 control-row 校验，再仅凭显式回复布尔值、父帖关系或精确 reply 类型移除回复，并重新映射剩余行，使回复不占用最终 `max_items`、不成为最新 item、也不推进来源水位。全为回复的可信 Dataset 是 `valid_empty`；引用、转发和无法由结构化字段判断关系的帖子继续保留。
- 证据边界：`replyCount` 只是互动量，正文以 `@` 或 `RT` 开头也可能是普通帖子，二者都不能作为过滤依据。输出过滤不能先于 placeholder/demo/error/control 校验，避免恶意或漂移行借回复标记伪装成合法空结果。Actor 输入支持关闭回复时可由 exact Schema/Manifest 另行优化，但不能成为唯一保证。
- 兼容与历史：本变更不增加 source config、API 字段、数据库 schema 或 migration，不改写现役 Candidate Manifest，也不重新抓取、删除或重写已有 Feed/History。当前及未来 Candidate 都在同一 X Adapter 边界执行该规则；其他平台与通用 Runtime 不增加平台分支。
- 原因：回复脱离父帖后在当前单卡 Feed 中缺少必要语境，却会占用付费抓取条数、分析和通知名额。把关系判断留在平台 Adapter，并让完整合同校验先于过滤，可以在不扩大权限、费用和历史写入范围的前提下提升 X 订阅信噪比。

<a id="d181"></a>
### D181 ActorOps 用来源级交叉验证、异步 repair 与安全执行链路维持健康

- 决策日期：2026-08-24
- 当前状态：本地实现与定向验证完成；未部署、未执行真实 Actor、付费抓取或迁移。
- 决策内容：global 31 新增来源×候选新鲜度、每来源单一未终态 repair 和安全执行事件。只有自然调度连续三次同一候选 `no_advance`，下一周期才优先使用备用交叉验证；备用推进时只来源级冷却原候选六小时并设新的来源首选，备用同样无推进确认没有新内容。24 小时内两个不同来源确认后才允许全局降级，且永不自动移除最后一路。全部候选已结算失败时当前 Job 保持失败、Feed/水位保持不变，异步 repair 在现有 Owner/Admin 站立授权、每 Probe `$0.05`、每 Route 每日 5 次、Workspace 每月 `$3.00` 内发现、探测并临时补位；未知费用、无授权、预算不足或无候选只记录稳定阻断码。
- 可观测性与边界：执行事件关联原 Job、控制 Job、Route/source/Candidate/repair，记录候选选择、启动、注册、结算、结果、切备和 repair 的稳定阶段、原因码、计数与最终费用，数据库保留 30 天并镜像到脱敏 JSONL。`GET /api/admin/apify-actor-events` schema 3 仅 Owner/Admin 可按 Job 等安全关联读取；不保存或返回目标、输入、正文、密钥、远端 Run/Dataset 或上游原始错误。任一 trace sink 失败不改变业务结果。
- 原因：单次 `no_advance` 无法区分真实静默和上游缓存，立刻全局替换会把一个来源的偶发差异扩大为整条路线的风险；同样，在费用或授权未知时“自动重试”会导致不可审计的重复付费。把判断限定到自然周期和来源级证据、把修复放到已有授权 Worker、把事实串回原 Job，既保证可解释也保留付费边界。

<a id="d182"></a>
### D182 系统运行参数采用 typed workspace override 与独立 Admin MCP 授权

- 决策日期：2026-08-24
- 当前状态：本地实现与定向验证完成；合并、推送、global 32 生产迁移和 VPS 发布由用户另行执行。
- 决策内容：把 21 项非秘密容量、Job、保留、存储和共享采集参数收口到唯一 typed registry；解析顺序固定为 workspace DB override、既有环境变量 alias、内置默认。global 32 只保存 override/generation 与十分钟 proposal，fresh DB 自动建立，已有数据库先完成 global 31 后必须停 API/Worker 显式备份迁移。Web `/settings/system` 与 Remote MCP 共用 preview、精确确认、单次消费和 generation CAS；运行时在对应 admission、新 Job、失败、维护、快照和采集边界热加载，不要求重启。
- 授权与隔离：新增 `system_settings_write` access、`inteliscope:system-settings:write` scope 与默认关闭的 `HORIZON_REMOTE_MCP_SYSTEM_SETTINGS_WRITES_ENABLED`。它只允许实时 Owner/Admin，和 read/subscription-write/workspace-diagnostics 相互独立，既有令牌不升级。Remote MCP 总工具数从 17 增至 20；系统连接只看 13 个读工具加 3 个系统工具，不获得订阅写入。
- 安全与非目标：密钥、Token、端点、数据库路径、Actor 费用/启用、任意环境变量和直接执行动作永久排除。日志只记 canonical changed key、数量、generation、proposal/result/request ID，不记值、alias 或确认短语。该能力不调用抓取、Actor、AI、通知，不改变付费授权边界。
- 取代关系：本决策取代 D177/D179 中“Remote MCP 精确 17 工具”和“配置工具一律不注册”的旧枚举；其 OpenClaw 专属模块边界、订阅 proposal、来源费用隔离和不引入通用 Agent Core 的其余约束继续有效。

<a id="d183"></a>
### D183 UI 默认继承以组件参数矩阵和语义令牌收口

- 决策日期：2026-08-24
- 当前状态：实现与定向 UI 契约验证完成；未安装 HeroUI Pro、未复制其代码、未新增实际颜色。
- 决策内容：未指定新风格的 UI 任务默认继承现有 Quiet Studio / graphite-purple 的组件角色与参数矩阵；用户明确指定的色彩或风格默认只作用于点名的页面/表面。HeroUI Pro Default 仅作为层级、密度和完成度参考；生产仅经 `frontend/src/design-system/**` 使用 HeroUI v3 OSS 与仓库自有实现。页面先选择既有角色，再决定变体、尺寸、间距、圆角、图标和动效；可复用的新参数只在设计系统与 UI 契约登记一次。桌面 SidebarRail 固定为 header / flexible nav / fixed account footer 三段；SettingsOperationCard、SettingsFormDialog 与 StatusPlacement 规定独立记录卡、表单对话框底部主操作和正常成功状态不单列的统一 anatomy。
- 可执行边界：业务代码不得复制 PageFrame 的 820/920/1180/960 px 宽度或 52 px 页头高度；字面量任意圆角、阴影和动效时长必须改用设计系统 CSS 变量；普通表单选择使用 HeroUI `Select`/`HeroSelect`，不直接渲染原生 `<select>`。22 px 表格、5 px 状态标记和 200 ms disclosure 保留既有数值并以语义令牌公开，避免这次治理改变暗色、亮色或三视口快照。颜色不采用 Tailwind 色板黑名单；可复用颜色仍只需进入语义主题。
- 后续细化：大量事件不再与主要配置纵向混排，管理页使用 URL 驱动的独立 Tab 并按 Tab 读取。可操作状态必须给出原因、影响、下一步与安全入口；Route 默认只保留决策信息，技术细节按需展开。侧栏滚动槽只在真实溢出时占位，固定账户底线不随展开态移动。
- 原因：此前主题、排版和少量页面模式已有约束，但“某类组件应该使用哪组参数”缺少单一、可查的默认矩阵，导致后续页面容易在按钮、卡片、图标、头部和动效上逐步漂移。把参考来源、默认继承、参数角色和少量高价值静态检查放在同一 UI 权威目录，可在不限制局部设计意图的前提下保持全站一致性。

<a id="d184"></a>
### D184 固定 RSSHub 运行密钥使用用途专用入口

- 决策日期：2026-08-24
- 当前状态：实现与定向 API/UI 验证完成；未执行真实抓取或外部请求。
- 决策内容：`RSSHUB_ACCESS_KEY` 保持固定运行用途，不成为可搜索、可误关联的通用 `SecretRef`。Owner/Admin 只在“设置 → 获取与主题”的 RSSHub 服务卡通过专用 GET/PUT/DELETE 管理其 write-only 值；状态只投影为 `secret_store`、`environment` 或 `none`，不返回真实值、任意变量名或通用密钥元数据。SecretStore 托管值可轮换或经明确风险提示删除；环境注入值只显示“环境托管”，页面返回稳定冲突而不覆盖或删除。写入/删除即时刷新 Service 环境，Worker 在下一周期重载。
- UI 与可访问性：RSSHub 卡按身份/状态、Base URL、边界说明分层，主地址保存不与密钥操作抢占同一 trailing 区域。商城预览采用共享 interactive Popover：触发标签与浮层构成一个可移入的交互区域，键盘、Escape、外部点击、焦点返回和触控点击均遵循同一收敛行为。
- 原因：RSSHub access code 不是 AI 或 Apify Key，跳往通用密钥页会制造一个无法实际管理的入口，也让用户误以为它能被重命名、绑定或任意删除。固定用途和专用状态能在保留 SecretStore 原子写入边界的同时，给出正确的运维责任和失败影响。

<a id="d185"></a>
### D185 付费平台订阅意图与来源就绪解耦并收紧默认获取量

- 决策日期：2026-08-25
- 当前状态：本地实现与定向验证完成；未部署、未执行真实 Actor、抓取、费用调用或 Binding 激活。
- 决策内容：Web“创建并订阅”与 D179 的 OpenClaw 语义一致，允许当前用户先保存对可访问 pending/disabled 平台来源的 enabled subscription；source、Binding 和 schedule 仍保持停用，不创建 Job、Attempt、抓取或费用。该例外只由 catalog 创建订阅入口显式声明，底层 REST mutation 的默认可见性检查、事务内二次检查和其他订阅写入口仍拒绝停用来源。
- 默认与兼容：Apify social 新建来源的 `fetch_limit` 默认从 20 收紧为 3，Web 平台表单、Agent 自服务规范化、兼容配置写入与运行模型保持一致；显式范围仍为 1..100。已有来源及其已保存获取条数不迁移、不改写，管理员核验、来源启用、Route/Candidate、配额和费用门槛不变。
- 原因：订阅表达的是用户意图，Binding/source 表达的是采集是否就绪；用 source enabled 同时代表两者会让平台来源创建成功后必然订阅 404。把两种状态分离可保留待核验隔离，同时避免丢失用户订阅。付费来源首次默认 20 条也高于常见阅读与验证需求，默认 3 条能降低无意结果量和费用暴露，用户仍可主动调高。

<a id="d186"></a>
### D186 平台 Binding 以本地证据自动收口并条件启用

- 决策日期：2026-08-25
- 当前状态：本地实现与定向验证完成；未执行真实 Actor、抓取、费用调用、AI 或通知。
- 决策内容：平台来源在创建、enabled subscription 保存/恢复、Route Candidate replacement apply 与 Worker housekeeping 时自动核验。证据仅为已保存的当前目标指纹、当前 active/standby Candidate 的 Manifest 完整性及 Adapter 输入兼容性，或同 Binding version 的 settled `valid_nonempty` Probe；不联网、不启动 Actor、不创建 Attempt/Job。证明通过时 Binding `pending → ready`，且仅在至少一个 enabled subscription 与允许执行的 Route 同时存在时启用 source；显式 disabled Binding 永不自动重启。Worker 每 workspace 每周期最多处理 100 条。
- 管理与可解释性：Admin 详情只投影安全来源名、source enabled、启用订阅数、可读状态与稳定 blocker，不返回 target、Manifest、Actor/Build、Run/Dataset 或原始错误。管理员可使用无确认短语的本地“重新检查准备中的来源”处理异常；保留原 verify 短语接口作为只处理 pending 的兼容 alias。来源表单和订阅 Toast 说明自动行为及“已启用/仍在准备”的差别。
- 取代关系：本决策取代 D179、D185 和其他现役合同中“管理员必须人工核验并启用新平台 Binding”的表述；付费 Candidate/Probe 的显式授权、Route CAS、配额与已结算费用证据不变。

<a id="d187"></a>
### D187 Apify 生产排空按 Key 角色隔离

- 决策日期：2026-08-25
- 当前状态：实现与定向验证完成；本地恢复仅允许原 validation Key 的只读 Run 窗口核验，不启动 Actor、不重放付费 Run。
- 决策内容：生产 Key 的 drain/failover 屏障只统计成员角色为 `acquisition` 的未终态 Actor Run，而不是按 Run 的 `purpose` 或工作区全量统计。validation 若临时借用 production acquisition Key，仍属于生产屏障并要 abort/确认终态；配置为专用 validation Key 的 Run 保留自身防重复启动锁定与审计，但永不阻塞 production standby 提升。此规则不增加 API、不迁移 schema，也不删除历史账本。
- 未知启动恢复：专用 validation Key 的 `start_outcome_unknown` 只以同一 validation Key 查询账户级 `/actor-runs` 的原创建时间窗。响应权威为空才终结为 `start_rejected`、`$0`、`charge_final`；窗口非空、HTTP/网络失败或解析歧义均保留未知记录，不推断远端 ID、不标记未启动、不 POST/abort/replay。该独立事实不改变 production Key 的 failover 判断。
- Worker 边界：每轮在领取 Job 前有界对账所有启用工作区；池未启用时零操作。单 workspace 对账失败只保留脱敏日志和安全状态，不能阻止免费来源或普通 Job 的领取；实际付费来源仍各自遵守现有 pool 状态、额度和费用准入。
- 原因：历史 dedicated-validation unknown-start 没有 remote Run 标识时，按全工作区统计会把一个只能人工/只读核验的审计事实错误扩大为生产 outage。按 credential role 划分失败域能同时保留未知费用的保守账本、避免猜测远端 Run，并恢复受影响生产 Key 的安全自动切换。
